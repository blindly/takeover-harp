/*
function coinClick(number) {
    coins = coins + number;
    document.getElementById("coins").innerHTML = accounting.formatMoney(coins);
};
*/

function workClick() {
    coins = coins + work;
    document.getElementById("coins").innerHTML = accounting.formatMoney(coins);
};